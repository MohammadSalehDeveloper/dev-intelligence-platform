# Commit explanations

Every proposed commit gets a file here. Agents never create the git commit; they write the explanation and give the user a message.

Use [../templates/commit.md](../templates/commit.md). Name files `YYYY-MM-DD-short-slug.md`.
